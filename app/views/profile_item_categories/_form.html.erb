<%= form_with(model: profile_item_category, local: true) do |form| %>
  <% if profile_item_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_item_category.errors.count, "error") %> prohibited this profile_item_category from being saved:</h2>

      <ul>
      <% profile_item_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
